mutation FlipCard($gameId: ID!, $cardId: ID!, $isTurned: Boolean!, $isMatched: Boolean!) {
  flipCard(gameId: $gameId, cardId: $cardId, isTurned: $isTurned, isMatched: $isMatched) {
        ...CardFragment
  }
}
